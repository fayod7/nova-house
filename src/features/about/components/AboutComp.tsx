import { memo } from 'react';

const AboutComp = () => {
  return (
    <section className='flex flex-col w-full'>
      <h2>Our Artistic Journey</h2>
    </section>
  );
};

export default memo(AboutComp);