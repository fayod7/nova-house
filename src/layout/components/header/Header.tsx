import { memo } from 'react';

const Header = () => {
  return (
    <header className='w-full'>
      <nav className="container">
        
      </nav>
    </header>
  );
};

export default memo(Header);